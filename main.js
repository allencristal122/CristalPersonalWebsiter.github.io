var typed = new Typed(".text", {
    // Array of strings to be typed out
    strings:["Student.", "Video Editor.", "Your Future Web Developer."],
    // Typing speed in milliseconds
    typeSpeed: 100,
    // Speed to erase previously typed characters in milliseconds
    backSpeed: 100,
    // Time delay in milliseconds before starting to erase
    backDelay:1000,
    // Whether to loop through the strings indefinitely
    loop: true
})